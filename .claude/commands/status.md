# Status Overview

Get a comprehensive status of all agents, their progress, and any blockers.

## Input
`$ARGUMENTS` = (none)

## Steps

1. **List all agent windows**:
   ```bash
   tmux list-windows -F "#{window_name}" | grep -v "^watch-"
   ```

2. **For each agent, capture output and parse**:
   ```bash
   tmux capture-pane -t "<name>" -p -S -50
   ```

3. **Check worktree status for each**:
   ```bash
   cd ../worktrees/<name> && git status --short && git log --oneline -3
   ```

4. **Produce comprehensive report**:

   ```
   ╔══════════════════════════════════════════════════════════════╗
   ║                    AGENT STATUS REPORT                       ║
   ╠══════════════════════════════════════════════════════════════╣

   SUMMARY: 3 agents | 2 working | 1 blocked | 0 complete

   ┌─────────────────────────────────────────────────────────────┐
   │ auth-service                                    [WORKING]   │
   ├─────────────────────────────────────────────────────────────┤
   │ Task: Implement OAuth2 login flow                           │
   │ Progress: Analyzing existing auth code                      │
   │ Commits: 0                                                   │
   │ Changed files: 0                                            │
   └─────────────────────────────────────────────────────────────┘

   ┌─────────────────────────────────────────────────────────────┐
   │ payment-api                                     [BLOCKED]   │
   ├─────────────────────────────────────────────────────────────┤
   │ Task: Add Stripe webhook handlers                           │
   │ Progress: Waiting for input                                 │
   │ QUESTION: Should webhooks use async processing or sync?     │
   │ Commits: 1 (initial setup)                                  │
   │ Changed files: 2                                            │
   └─────────────────────────────────────────────────────────────┘

   ┌─────────────────────────────────────────────────────────────┐
   │ user-service                                    [WORKING]   │
   ├─────────────────────────────────────────────────────────────┤
   │ Task: Add user profile endpoints                            │
   │ Progress: Writing API handlers                              │
   │ Commits: 2                                                   │
   │ Changed files: 4                                            │
   └─────────────────────────────────────────────────────────────┘

   ⚠️  ACTION REQUIRED:
   - payment-api needs answer: "Should webhooks use async processing or sync?"

   💡 SUGGESTIONS:
   - /tell payment-api Use async with a job queue for reliability
   - /check auth-service for detailed progress
   ```

5. **If any agents have questions, highlight them prominently**
